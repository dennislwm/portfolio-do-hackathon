spec:
  name: portfolio-do-hackathon
  static_sites:
  - build_command: rm -r ./public; hugo --minify --destination ./public
    environment_slug: hugo
    git:
      branch: main
      repo_clone_url: https://github.com/dennislwm/portfolio-do-hackathon.git
    name: portfolio-do-hackathon
